# This project uses TWO env files:
#   backend/.env   -> copy from backend/.env.example  (holds OPENROUTER_API_KEY)
#   frontend/.env  -> copy from frontend/.env.example (holds VITE_API_BASE_URL)
#
# This root file is just a pointer/reminder — see README.md "Setup" section.
